export enum ROLE {
  GUEST = 'guest',
  USER = 'user',
  DELIVERYMAN = 'deliveryman',
  OPERATOR = 'operator',
  MANAGER = 'manager',
  ADMIN = 'admin',
}

export const ROLE_ARRAY = Object.values(ROLE);
